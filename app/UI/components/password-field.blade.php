<div class="form-field">
    <hx-password-control>
        <input
            id="{{$id}}"
            name="{{$id}}"
            type="password"
        />
        <label
            for="{{$id}}">
            {{$label}}
        </label>
    </hx-password-control>

</div>
